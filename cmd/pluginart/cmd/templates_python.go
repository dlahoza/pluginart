package cmd

const pyWireTmpl = `# Code generated by pluginart. DO NOT EDIT.
# Wire protocol: framing, FlatBuffers handshake, serve() and connect helpers.
import os
import socket
import struct
import sys
import threading

import flatbuffers
import flatbuffers.encode
import flatbuffers.packer
import flatbuffers.table

_MAGIC    = b'PLGN'
_HDR_SIZE = 9  # 4 magic + 4 length + 1 flags

_MSG_HANDSHAKE_REQUEST  = 0x01
_MSG_HANDSHAKE_RESPONSE = 0x02
_MSG_CALL_REQUEST       = 0x03
_MSG_CALL_RESPONSE      = 0x04
_MSG_PLUGIN_ERROR       = 0x05
_MSG_CANCEL             = 0x06
_MSG_PING               = 0x07
_MSG_PONG               = 0x08

CONTRACT_HASH = '{{.ContractHash}}'


def _recv_frame(sock: socket.socket) -> tuple[int, bytes]:
    hdr = b''
    while len(hdr) < _HDR_SIZE:
        chunk = sock.recv(_HDR_SIZE - len(hdr))
        if not chunk:
            raise ConnectionError('connection closed')
        hdr += chunk
    if hdr[:4] != _MAGIC:
        raise ValueError(f'bad magic: {hdr[:4]!r}')
    length, flags = struct.unpack('<IB', hdr[4:])
    payload = b''
    while len(payload) < length:
        chunk = sock.recv(length - len(payload))
        if not chunk:
            raise ConnectionError('connection closed mid-payload')
        payload += chunk
    return flags, payload


def _send_frame(sock: socket.socket, flags: int, payload: bytes) -> None:
    hdr = _MAGIC + struct.pack('<IB', len(payload), flags)
    sock.sendall(hdr + payload)


def _parse_contract_hash(buf: bytes) -> str:
    ba = bytearray(buf)
    root_pos = flatbuffers.encode.Get(flatbuffers.packer.uoffset, ba, 0)
    tab = flatbuffers.table.Table(ba, root_pos)
    o = tab.Offset(4)
    return tab.String(o + tab.Pos).decode('utf-8') if o else ''


def _build_handshake_response(ok: bool, error: str = '') -> bytes:
    b = flatbuffers.Builder(64)
    err_off = b.CreateString(error) if error else 0
    b.StartObject(2)
    b.PrependBoolSlot(0, ok, 0)
    if err_off:
        b.PrependUOffsetTRelativeSlot(1, err_off, 0)
    root = b.EndObject()
    b.Finish(root)
    return bytes(b.Output())


def _build_handshake_request(contract_hash: str, plugin_name: str) -> bytes:
    b = flatbuffers.Builder(128)
    ch = b.CreateString(contract_hash)
    pn = b.CreateString(plugin_name)
    b.StartObject(3)
    b.PrependUOffsetTRelativeSlot(0, ch, 0)
    b.PrependUOffsetTRelativeSlot(1, pn, 0)
    b.PrependUint16Slot(2, 1, 0)
    root = b.EndObject()
    b.Finish(root)
    return bytes(b.Output())


def _parse_handshake_response(buf: bytes) -> tuple[bool, str]:
    ba = bytearray(buf)
    root_pos = flatbuffers.encode.Get(flatbuffers.packer.uoffset, ba, 0)
    tab = flatbuffers.table.Table(ba, root_pos)
    o4 = tab.Offset(4)
    ok = bool(ba[tab.Pos + o4]) if o4 else False
    o6 = tab.Offset(6)
    error = tab.String(o6 + tab.Pos).decode('utf-8') if o6 else ''
    return ok, error


def connect(sock: socket.socket, plugin_name: str) -> None:
    """Perform client-side handshake. Raises on failure."""
    _send_frame(sock, _MSG_HANDSHAKE_REQUEST, _build_handshake_request(CONTRACT_HASH, plugin_name))
    flags, payload = _recv_frame(sock)
    if flags != _MSG_HANDSHAKE_RESPONSE:
        raise RuntimeError(f'unexpected frame during handshake: {flags:#x}')
    ok, error = _parse_handshake_response(payload)
    if not ok:
        raise RuntimeError(f'handshake rejected: {error}')


def call(sock: socket.socket, payload: bytes) -> bytes:
    """Send a CallRequest and return the CallResponse payload."""
    _send_frame(sock, _MSG_CALL_REQUEST, payload)
    flags, resp = _recv_frame(sock)
    if flags == _MSG_PLUGIN_ERROR:
        raise RuntimeError(f'plugin error: {resp!r}')
    if flags != _MSG_CALL_RESPONSE:
        raise RuntimeError(f'unexpected frame type: {flags:#x}')
    return resp


def _handle_conn(conn: socket.socket, handler) -> None:
    try:
        flags, payload = _recv_frame(conn)
        if flags != _MSG_HANDSHAKE_REQUEST:
            return
        if _parse_contract_hash(payload) != CONTRACT_HASH:
            got = _parse_contract_hash(payload)
            _send_frame(conn, _MSG_HANDSHAKE_RESPONSE,
                        _build_handshake_response(False, f'contract mismatch: got {got!r}'))
            return
        _send_frame(conn, _MSG_HANDSHAKE_RESPONSE, _build_handshake_response(True))
        while True:
            flags, payload = _recv_frame(conn)
            if flags == _MSG_CALL_REQUEST:
                try:
                    _send_frame(conn, _MSG_CALL_RESPONSE, handler(payload))
                except Exception as exc:
                    _send_frame(conn, _MSG_PLUGIN_ERROR, str(exc).encode())
            elif flags == _MSG_PING:
                _send_frame(conn, _MSG_PONG, payload)
            elif flags == _MSG_CANCEL:
                pass
            else:
                break
    except (ConnectionError, OSError):
        pass
    finally:
        conn.close()


def serve(handler) -> None:
    """Start the plugin server. handler(payload: bytes) -> bytes."""
    sock_path = os.environ.get('PLUGIN_SOCKET', '')
    addr_str  = os.environ.get('PLUGIN_ADDR', '')

    if sock_path:
        srv = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
        srv.bind(sock_path)
    elif addr_str:
        host, port = addr_str.rsplit(':', 1)
        srv = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        srv.bind((host, int(port)))
    else:
        print('PLUGIN_SOCKET or PLUGIN_ADDR must be set', file=sys.stderr)
        sys.exit(1)

    srv.listen(128)
    print('READY', flush=True)

    while True:
        conn, _ = srv.accept()
        threading.Thread(target=_handle_conn, args=(conn, handler), daemon=True).start()
`

const pyPluginTmpl = `# Code generated by pluginart. DO NOT EDIT.
from pluginart import serve
from contract import CONTRACT_HASH
from handler import handle

if __name__ == '__main__':
    serve(handle, contract_hash=CONTRACT_HASH)
`

const pyClientTmpl = `# Code generated by pluginart. DO NOT EDIT.
from pluginart import PluginManager


class {{.Namespace}}Client:
    """Typed host-side client for the {{.Namespace}} plugin."""

    def __init__(self, manager: PluginManager, plugin_name: str) -> None:
        self._manager = manager
        self._plugin_name = plugin_name
{{range .Methods}}
    def {{.Name}}(self, payload: bytes) -> bytes:
        """Call {{.Name}} on the plugin. payload is a full schema CallRequest FlatBuffer."""
        return self._manager.call(self._plugin_name, payload)
{{end}}`

const pyContractTmpl = `# Code generated by pluginart. DO NOT EDIT.
CONTRACT_HASH = '{{.ContractHash}}'
`

const pyHandlerTmpl = `# Edit this file to implement your plugin logic.

def handle(payload: bytes) -> bytes:
    """payload is a raw FlatBuffers CallRequest. Return raw FlatBuffers CallResponse bytes."""
    raise NotImplementedError
`

const pyRequirementsTmpl = `pluginart==0.2.0
flatbuffers>=24.3.25
`

const pyDockerfileTmpl = `FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENTRYPOINT ["python", "plugin.py"]
`
